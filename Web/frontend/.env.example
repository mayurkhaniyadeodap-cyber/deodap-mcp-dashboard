# --- DeoDap MCP Dashboard · Frontend env ---
# Copy to .env. The app calls the API via this base URL. In dev, leave it as
# "/api" so the Vite proxy (vite.config.ts) forwards to the FastAPI server and
# no host is hard-coded anywhere in the frontend.
VITE_API_BASE_URL=/api

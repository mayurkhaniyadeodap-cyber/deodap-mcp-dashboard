# DeoDap MCP Dashboard — backend dependencies
# Mirrors pyproject.toml (pinned to the versions in the project venv), plus the
# HTTP client + official MCP SDK used for the Phase-2 live integration.
# Install:  pip install -r requirements.txt

# --- Web framework / server ---
fastapi==0.139.0
uvicorn[standard]==0.51.0

# --- Validation / settings ---
pydantic[email]==2.13.4
email-validator==2.3.0
pydantic-settings==2.14.2

# --- Auth ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1            # pinned <4.1 for passlib 1.7.4 compatibility
python-multipart==0.0.32

# --- Database (structure only in Phase 1) ---
SQLAlchemy==2.0.51
alembic==1.18.5

# --- Export ---
openpyxl==3.1.5

# --- MCP integration (Phase 2) ---
httpx>=0.27
mcp>=1.0
